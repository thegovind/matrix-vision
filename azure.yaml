# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: vla-learning-lab
metadata:
  template: vla-learning-lab@0.0.1

services:
  web:
    project: ./
    language: ts
    host: containerapp
    docker:
      path: ./Dockerfile
      context: ./
      remoteBuild: true
